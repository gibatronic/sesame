#!/usr/bin/env bash

. tsk/env

main() {
  local OPTIONS=(
    --upload
    --board $ARDUINO_BOARD
    --port $ARDUINO_PORT
    --pref build.path="${SESAME_PATH}/bin"
    "${SESAME_PATH}/src/main.ino"
  )

  $ARDUINO "${OPTIONS[@]}"
}

main
