#!/usr/bin/env bash

ARDUINO='/Applications/Arduino.app/Contents/MacOS/Arduino'
ARDUINO_BOARD='arduino:avr:leonardo'
ARDUINO_PORT="$(find /dev/cu.usbmodem* | head -n 1)"

SESAME_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
